<nav class="navbar bg-white py-1 w-full shadow-sm sticky top-0 z-50">
  <div class="container mx-auto flex items-center justify-between">
    <!-- Logo and title -->
    <a href="/neighborhoods" class="flex items-center space-x-1 text-blue-800 text-xl">
      <img src="/images/p-logo.jpg" alt="Testimonial 3" class="w-16 h-16 rounded-full object-cover">
      <span class="title font-bold">Neighborhood Hub</span>
    </a>

    <!-- Burger Menu Icon -->
    <div class="md:hidden flex items-center">
      <button id="burger" class="focus:outline-none">
        <svg class="burger w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>

    <!-- Links -->
    <div class="links hidden md:flex items-center space-x-6 font-semibold text-sm">
      <a href="/neighborhoods" class="text-black hover:text-blue-500"><i class="fas fa-city mr-2"></i>Neighborhoods</a>
      <a href="/neighborhoods/new" class="text-black"><i class="fas fa-circle-plus mr-2"></i>Add Neighborhood</a>
      <a href="/neighborhoods/popular" class="text-black"><i class="fas fa-crown mr-2"></i>Popular</a>
      <% if(currentUser) {%>
        <a href="/neighborhoods/favourites" class="fav">
          <i class="fas fa-heart-circle-check text-xl text-blue-600 mr-2"></i>  Favourites 
      </a>    
  
        <% } else {%>
          <p class="fav-login">
            <i class="fas fa-heart-circle-check text-xl text-blue-600 mr-2"></i>  Favourites 
          </p>    
          <% } %>
    
      <% if(!currentUser) { %>
        <a href="/login" class="text-black">Login</a>
      <% } %>
      <% if(currentUser) { %>
        <a href="/logout" class="text-black">Logout</a>
      <% } %>

      <!-- Search Bar (Desktop) -->
      <div class="relative">
        <input type="search" name="q" id="search-desktop" placeholder="Search neighborhood"
          class="px-4 py-1 pl-12 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 text-black w-full">
        <span class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
          <i class="fas fa-search text-gray-400 ml-2"></i>
        </span>
        <div id="suggestions-desktop" class="absolute bg-white border border-gray-300 mt-1 w-full hidden rounded-md z-10"></div>
      </div>      
    </div>
  </div>
  <!-- Collapsible Links for Mobile -->
  <div id="mobile-menu" class="mobile-links hidden md:hidden flex flex-col space-y-2 mt-3 ml-3 font-semibold text-sm">
    <a href="/neighborhoods" class="active text-black"><i class="fas fa-city mr-2"></i>Neighborhoods</a>
    <a href="/neighborhoods/new" class="text-black"><i class="fas fa-circle-plus mr-2"></i>Add Neighborhood</a>
    <a href="/neighborhoods/popular" class="text-black"><i class="fas fa-crown mr-2"></i>Popular</a>
    <% if(currentUser) {%>
      <a href="/neighborhoods/favourites" class="fav">
        <i class="fas fa-heart-circle-check text-lg text-blue-600 mr-2"></i>Favourites 
    </a>    

      <% } else {%>
        <p class="fav-login">
          <i class="fas fa-heart-circle-check text-xl text-blue-600 mr-2"></i>  Favourites 
        </p>

        <% } %>
  
    <% if(!currentUser) { %>
      <a href="/login" class="text-black">Login</a>
    <% } %>
    <% if(currentUser) { %>
      <a href="/logout" class="text-black">Logout</a>
    <% } %>

    <!-- Search Bar (Mobile) -->
    <div class="relative">
      <input type="search" name="q" id="search-mobile" placeholder="Search neighborhood"
        class="px-4 py-1 pl-12 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 text-black w-full">
      <span class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
        <i class="fas fa-search text-gray-400 ml-2"></i>
      </span>
      <div id="suggestions-mobile" class="absolute bg-white border border-gray-300 mt-1 w-full hidden rounded-md z-10"></div>
    </div>
  </div>
</nav>

<script src="/javascripts/nav.js"></script>
<script src="/javascripts/search.js"></script>

